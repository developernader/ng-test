import { HeroDropdownDirective } from './hero-dropdown.directive';

describe('HeroDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
